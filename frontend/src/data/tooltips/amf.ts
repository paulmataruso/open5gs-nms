// AMF (Access and Mobility Management Function) Tooltips
export const AMF_TOOLTIPS = {
  sbi_address: "Bind address for the AMF's Service-Based Interface — the HTTP/2 API other 5G core NFs use to reach the AMF (e.g. SMF calling back over N11 during PDU session establishment, PCF pushing policy updates). Distinct from the NGAP address below, which is the RAN-facing side — this one is core-internal only. Use a dedicated loopback alias (e.g. 127.0.0.5) so it never collides with another NF's SBI port on this same host.",
  sbi_port: "Port for the AMF's SBI. Default: 7777, same default as every other 5G NF — safe because each NF gets its own bind address. Used for AMF-initiated and AMF-received SBI calls to/from SMF, PCF, NSSF, and the NRF itself.",
  scp_uri: "URI of a Service Communication Proxy to route AMF's outbound SBI calls through indirect communication mode instead of resolving peers directly via NRF. Leave empty for direct mode (the default and simplest setup for a single-host or small deployment) — only set this if you've actually deployed an SCP and want AMF's NF-to-NF traffic proxied/load-balanced through it. Format: http://ip:port.",
  ngap_address: "Bind address for the N2 (NGAP) interface — where 5G base stations (gNodeBs) connect for control-plane signaling: UE registration, PDU session setup/release, handover, and paging. This is the RAN-facing address and must be reachable from every gNodeB's own N2 configuration (via routable IP, VPN, or SecGW tunnel) — if a gNodeB can't reach this address the cell will never bring up NGAP and no UE served by it can register at all.",
  ngap_port: "NGAP (N2) port gNodeBs connect to. Default: 38412, transported over SCTP (not TCP/UDP) — gNodeB configuration must match this exact port and protocol or the SCTP association will never establish, which typically shows as the cell staying stuck in a 'not connected to core' state on the radio's own management UI.",
  guami_mcc: "Mobile Country Code component of this AMF's GUAMI (Globally Unique AMF Identifier), broadcast to UEs/gNodeBs as part of the AMF's identity so a UE can be routed back to the same AMF on reselection. Must match the PLMN broadcast by the RAN and provisioned on subscriber SIMs/eSIMs — a mismatch here causes registration rejects with a PLMN-not-allowed style cause code.",
  guami_mnc: "Mobile Network Code component of this AMF's GUAMI. Combined with guami_mcc this forms the PLMN half of the AMF's globally unique identity; the region/set/pointer fields below complete it. Must be consistent with every other NF's PLMN configuration in this deployment.",
  guami_region: "AMF Region ID (8-bit, 0-255) — the top-level geographic/administrative grouping within GUAMI, intended for operators running AMF pools across multiple regions. In a small or single-site deployment this can simply stay at its default; it only becomes meaningful once you're deliberately partitioning AMF capacity by region for large-scale redundancy.",
  guami_set: "AMF Set ID (10-bit, 0-1023) — groups multiple AMF instances within a region into a pool for load distribution and N2-handover-friendly redundancy (a UE registered against one AMF in a set can be relocated to another in the same set without a full re-registration). Only relevant once you're running more than one AMF instance; a single-AMF deployment can leave this at its default.",
  guami_pointer: "AMF Pointer (6-bit, 0-63) — uniquely identifies this specific AMF instance within its region+set, completing the GUAMI. Must be unique among any other AMF sharing the same region/set values; with only one AMF in this deployment, any legal value works since there's nothing to collide with.",
  tai_mcc: "Mobile Country Code for this AMF's Tracking Area Identity — must match the PLMN actually broadcast by the connected gNodeBs. This is a separate MCC field from guami_mcc even though they're normally kept identical; a mismatch between the two is a subtle, easy-to-miss misconfiguration.",
  tai_mnc: "Mobile Network Code for this AMF's Tracking Area Identity, paired with tai_mcc. Must be consistent with the network identity broadcast by the RAN — used together with the TAC below to define which UEs' registration/paging this AMF is responsible for.",
  tai_tac: "Tracking Area Code (1-16777215 in 5G, wider range than 4G's TAC) — groups gNodeB cells into tracking areas for paging and mobility-registration-update efficiency. A larger TAC value has no functional meaning by itself; what matters is how many distinct TAC values you use: fewer/larger tracking areas means less signaling from Tracking Area Updates but more paging traffic per idle-UE incoming call/message (paging fans out to every cell in the TA), since the network doesn't know which specific cell an idle UE is under.",
  plmn_mcc: "Mobile Country Code this AMF advertises as one of its supported PLMNs (PLMN Support List) — tells gNodeBs and the discovery layer which national networks this AMF is willing to serve. Must include the MCC actually broadcast by every gNodeB expected to route registrations to this AMF.",
  plmn_mnc: "Mobile Network Code this AMF advertises in its PLMN Support List, paired with plmn_mcc. Must match what's provisioned on subscriber SIM/eSIM profiles — a UE's SIM only attempts to register on PLMNs it considers home/allowed/equivalent, so a mismatch here silently prevents attach with no obvious error on the AMF side.",
  plmn_sst: "Slice Service Type (1-255) advertised for this PLMN entry — declares which 3GPP-standard slice categories this AMF supports. Standard values: 1 = eMBB (enhanced Mobile Broadband — regular high-throughput data, the default for almost every deployment), 2 = URLLC (Ultra-Reliable Low-Latency Communication — industrial/time-critical use cases), 3 = MIoT (massive IoT — high device density, low per-device throughput). A subscriber's own S-NSSAI must match one of the SSTs advertised here or their PDU session request for that slice will be rejected.",
  plmn_sd: "Slice Differentiator (optional 24-bit hex value, e.g. 0x123456) — lets you run multiple independent slices that share the same SST (e.g. two separate eMBB slices for different tenants/customers). Leave unset if you're not deliberately operating multiple slices of the same SST; most deployments only need the SST alone.",
  integrity_order: "Ordered list of NAS integrity-protection algorithms the AMF will offer during the UE security-mode procedure — the first algorithm in this list that the UE also supports wins. NIA2 (AES-based) is the modern, hardware-accelerated recommendation and should generally be listed first; NIA0 (null integrity) should only ever be included for lab/interop testing, never a real deployment, since it means NAS signaling has zero integrity protection.",
  ciphering_order: "Ordered list of NAS encryption algorithms offered during security-mode setup, protecting the confidentiality of NAS signaling (not user-plane data — that's a separate PDCP-layer negotiation). NEA2 (AES) is the recommended first choice for production; NEA0 (null encryption) should be reserved for controlled testing where you deliberately want to inspect plaintext NAS in a packet capture.",
  network_name_full: "Full operator/network name shown to the user during 5G registration and in phone settings (max 63 characters). Purely cosmetic/informational — has no effect on attach, routing, or any protocol behavior — but a blank or wrong value here is often the first thing a user notices when checking whether they're on the right network.",
  network_name_short: "Abbreviated network name (max 31 characters), shown by UEs whose display doesn't have room for the full name. Same cosmetic-only scope as network_name_full — set both consistently so users see a sensible name regardless of which one their specific device chooses to render.",
  amf_name: "This AMF instance's own identifier, in FQDN format (e.g. amf1.5gc.mnc001.mcc001.3gppnetwork.org). Used internally for logging/debugging correlation and by other NFs' NF-management calls when referring to this specific AMF instance — distinct from any DNS record; this string doesn't need to actually resolve unless you're also using it as an SBI advertise hostname elsewhere.",
  log_path: "Filesystem path where the AMF writes its log file — registration attempts, handovers, NGAP setup/release, and NAS-layer errors all land here. The parent directory must exist and be writable by the open5gs-amfd process, or the service fails to start rather than silently skip logging.",
  log_level: "AMF log verbosity. debug shows essentially every UE interaction (registration, service request, handover, paging) — the right setting when actively chasing a specific attach/mobility problem, but very high-volume on a busy network. info is the standard steady-state setting. warn/error only surface real failures once you've confirmed normal operation and just want signal, not noise.",
};
