// Auto-Config Tooltips
export const AUTO_CONFIG_TOOLTIPS = {
  network_name: "The operator/network name displayed to end users on their device during registration and in phone network settings (max 63 characters). Purely cosmetic — has no effect on attach success or any protocol behavior — but a wrong or blank value is often the first thing a user notices when checking they're on the intended network. Keep it concise; long names may get truncated by the UE's own display.",
  mcc: "Mobile Country Code — 3 digits identifying the country, applied across every core NF this Auto-Config flow provisions in one pass. Examples: 001 = test networks, 310-316 = USA, 234 = UK, 262 = Germany, 460 = China. Changing this after radios are already provisioned requires re-pushing config to every radio too (their own broadcast PLMN must match), not just the core — a core-only MCC change without a matching radio update will make every UE unable to attach.",
  mnc: "Mobile Network Code — 2-3 digits identifying your specific operator within the country named by mcc. Must be coordinated (unique, or deliberately matching a real operator for private-network interop reasons) — applied consistently across every core NF this wizard touches.",
  ue_ip_pool: "IPv4 address pool (CIDR notation, e.g. 10.45.0.0/16) that every UE across the network draws its session IP from. Size this generously above your actual expected peak concurrent users — running out of addresses causes new PDU sessions/PDN connections to fail even though every other part of the network is healthy, and resizing this pool later after subscribers are already using addresses from it is disruptive.",
  dns_primary: "Primary DNS server address handed to every UE for hostname resolution once their data session is up. Common choices: 8.8.8.8 (Google Public DNS), 1.1.1.1 (Cloudflare) — pick a resolver that's actually reachable from wherever UE traffic egresses to the internet; an unreachable DNS server here silently breaks name resolution for every app on every subscriber's device, even though the session itself looks perfectly healthy.",
  dns_secondary: "Secondary/fallback DNS server used if the primary is unreachable or slow. Recommended to be a genuinely independent provider from the primary (e.g. pairing 8.8.8.8 with 1.1.1.1 rather than two Google addresses) so a single upstream provider outage doesn't take down DNS for the whole network.",
  amf_address: "AMF's NGAP server IP — where 5G base stations (gNodeBs) connect for N2 control-plane signaling. Must be reachable by every gNodeB expected to serve this core, whether over flat LAN, VPN, or a SecGW/IPsec tunnel; unreachable here means a gNodeB never even completes NGAP setup, so no UE under it can register at all.",
  upf_address: "UPF's GTP-U address for user-plane traffic — where gNodeBs actually send/receive subscriber IP packets after GTP-U encapsulation/decapsulation. This is a completely separate path from amf_address's control-plane signaling; a healthy AMF connection does not guarantee this address is also reachable, and if it isn't, UEs can register successfully while all their real data traffic silently goes nowhere.",
  mme_address: "MME's S1-AP server IP — where 4G base stations (eNodeBs) connect for control-plane signaling (attach, bearer setup, handover, paging). Must be reachable by every eNodeB over SCTP; a firewall/NAT that mishandles SCTP (common, since it's a less familiar protocol than TCP/UDP) can silently block this even when the address responds to plain ICMP ping.",
  s1mme_ip: "MME's S1-MME interface IP address — eNodeBs connect here for 4G control-plane signaling (UE attach, bearer setup, paging), over SCTP port 36412. This is the single most important address for 4G RAN connectivity: if a radio can't reach it, it never completes S1 Setup and stays permanently disconnected from the core, regardless of how correct every other radio setting is.",
  sgwu_gtpu_ip: "SGW-U's GTP-U server IP for the S1-U interface — receives user-plane data tunnels directly from eNodeBs, over UDP port 2152. Distinct from s1mme_ip's control-plane role; a working S1-MME connection doesn't imply this address is reachable too, and if it isn't, UEs attach fine but get no actual data throughput.",
  amf_ngap_ip: "AMF's NGAP server IP for the N2 interface — gNodeBs connect here for 5G control-plane signaling (registration, session management, handover), over SCTP port 38412. The 5G equivalent of s1mme_ip; same SCTP-specific reachability caveat applies (a firewall that only understands TCP/UDP can silently drop SCTP while still passing ping).",
  upf_gtpu_ip: "UPF's GTP-U server IP for the N3 interface — receives user-plane data directly from gNodeBs (and also handles N9, UPF-to-UPF forwarding, when applicable). The 5G equivalent of sgwu_gtpu_ip; must be reachable from every gNodeB or PDU sessions establish successfully at the control-plane level while carrying zero actual data.",
  session_pool_ipv4_subnet: "IPv4 address pool (CIDR, e.g. 10.45.0.0/16) the UPF assigns from for UE PDU sessions. Size for your real concurrent-user peak with meaningful headroom — this pool is shared across every subscriber attaching to this DNN, and exhausting it silently starts failing new session requests network-wide, not just for one user.",
  session_pool_ipv4_gateway: "IPv4 gateway address for the session pool above — what UEs see as their default route, typically the first usable address in session_pool_ipv4_subnet (e.g. 10.45.0.1 for a 10.45.0.0/16 pool). This is also where the UPF terminates the corresponding TUN interface on this host.",
  session_pool_ipv6_subnet: "IPv6 address pool (CIDR, e.g. 2001:db8:cafe::/48) used for IPv6 PDU sessions on this DNN. Only relevant if you're actually offering IPv6 or dual-stack (IPv4v6) sessions — a UE requesting IPv6 against a DNN with no IPv6 pool configured here will fail or fall back to IPv4-only depending on negotiation behavior.",
  session_pool_ipv6_gateway: "IPv6 gateway address for the IPv6 session pool — the default route IPv6-capable UEs use for this DNN, conventionally the first address in session_pool_ipv6_subnet.",
  nat_interface: "Name of the local network interface used for NAT masquerading of UE traffic heading toward the internet. Default: ogstun — the TUN/TAP interface Open5GS's own UPF creates automatically for UE data-plane traffic. If you're running a non-default UPF setup (multiple DNNs each with their own named TUN interface, e.g. ogstun2/ogstun3) this needs to reference the correct interface for the DNN this NAT rule applies to, not always the default.",
};
