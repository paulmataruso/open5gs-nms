// NRF (Network Repository Function) Tooltips
export const NRF_TOOLTIPS = {
  sbi_address: "Bind address for the NRF's Service-Based Interface — the HTTP/2 API every other 5G NF (AMF, SMF, PCF, UDM, etc.) calls to register itself and to discover peers. The NRF is the root of 5G service discovery: if this address is wrong or unreachable, every other NF's own NRF registration fails at startup and they fall back to whatever static peer config they have (often none), so this is one of the most load-bearing addresses in the whole core. Use 127.0.0.10 (or another loopback alias) when every NF runs on this same host — Open5GS's own convention is one dummy/loopback IP per NF so they never collide on port 7777. Use a real routable IP only if NFs on other hosts need to register with this NRF.",
  sbi_port: "Service-Based Interface port the NRF listens on. Default: 7777 — the same default every other 5G NF uses for its own SBI, which is exactly why each NF needs its own distinct bind address (see sbi_address) rather than all sharing one IP on this port. Changing it away from 7777 only matters if something else on this host is already bound to that port; if you do change it, every other NF's nrf_uri/discovery config pointing at this NRF must be updated to match, or they'll fail to register.",
  serving_mcc: "Mobile Country Code for the PLMN this NRF serves — 3 digits identifying the country per ITU-T E.212 (e.g. 001 for test networks, 310 for USA). The NRF uses this to scope which NF profiles it considers 'local' to this PLMN when other NFs query it for discovery. Must match the MCC configured on every other core NF and broadcast by the RAN — a mismatch here doesn't crash anything by itself, but can cause NF discovery to return no matches for cross-PLMN queries.",
  serving_mnc: "Mobile Network Code for the PLMN this NRF serves — 2-3 digits identifying the specific operator within the country named by serving_mcc. Combined with MCC this forms the full PLMN ID (e.g. 001-01) that ties every NF's registration together. Must be consistent across every NF's own PLMN config in this deployment.",
  log_path: "Filesystem path where the NRF process writes its log file. The parent directory must already exist and be writable by the user the open5gs-nrfd systemd service runs as — a missing/unwritable directory causes the NRF to fail at startup with a log-open error, not a silent skip. Useful for diagnosing NF registration failures: every PUT/DELETE to /nnrf-nfm/v1/nf-instances shows up here.",
  log_level: "NRF logging verbosity. debug/trace show every NF registration, heartbeat, and discovery query — invaluable when tracking down why a specific NF isn't showing up in discovery, but very high-volume in a busy core. info is the right default for a stable, working deployment. warn/error only surface real problems (failed registrations, malformed requests) and are appropriate once you've confirmed discovery is working and just want the noise gone.",
};
