// SIM Generator Tooltips
export const SIM_GENERATOR_TOOLTIPS = {
  mcc: "Mobile Country Code — 3 digits identifying the country, baked into every generated IMSI's first 3 digits. Must exactly match this network's own core-side PLMN configuration (Config → the core NF pages), or every SIM this generator produces will attach to the wrong (or no) network the moment it's inserted into a device, since the IMSI itself encodes the PLMN. Examples: 001 = test networks, 310-316 = USA, 234 = UK.",
  mnc: "Mobile Network Code — 2-3 digits identifying the specific operator within the country named by mcc, baked into the generated IMSI right after the MCC. Must match your network's own configured MNC — pick a value coordinated with your actual deployment, not an arbitrary one, since a mismatch here is identical in effect to the MCC mismatch above (silent attach failure for every generated SIM).",
  issuer: "Issuer Identifier Number component of the generated ICCID (SIM card serial number) — typically 2-3 digits identifying your organization as the card issuer, appearing right after the country code in the ICCID structure. This has no effect on network attach (unlike MCC/MNC above, which are baked into the IMSI) — it only affects the SIM's own physical serial-number identity, useful for inventory/batch tracking.",
  count: "How many SIM credential sets to generate in this batch (1-100). Each one gets its own independently unique ICCID, IMSI, Ki, and OPc — generating a batch does not reuse or derive values from previous batches, so there's no risk of accidental credential collision between separate generation runs.",
  sequential_imsi: "When enabled, generates consecutive IMSI numbers (…0001, …0002, …0003, and so on from the starting MSIN) instead of randomly distributed ones. Makes inventory tracking and bulk provisioning much easier to manage (you know exactly which IMSI range a given batch occupies), at the cost of making the IMSI sequence itself predictable — a minor consideration for an internal/private test network, more relevant if these SIMs will ever face any adversarial scrutiny.",
  starting_msin: "The first Mobile Subscriber Identification Number to use when Sequential IMSI is enabled; every subsequent SIM in the batch increments from this value. Must fit within 15 minus the combined digit length of MCC and MNC (e.g. with a 3-digit MCC and 2-digit MNC, the MSIN has exactly 10 digits available) — entering a value with the wrong digit count for your MCC/MNC combination will either get rejected or silently produce an invalid-length IMSI, so double-check the math when changing MCC/MNC from their defaults.",
  custom_adm: "Sets one shared Master Administrative Key (ADM1, 16 hex characters / 64-bit) across every SIM in this batch instead of generating a unique one per SIM. ADM1 is the key that unlocks administrative write access to the SIM's file system (needed for advanced reprovisioning). Sharing one ADM1 across a batch is convenient for lab/test SIMs where you'll be reprogramming them by hand, but is a real security downgrade for anything resembling a production deployment — leave this unchecked so each SIM gets its own unique, independently-secure ADM1.",
  custom_pin: "Sets one shared PIN1 (4-8 digits) across every SIM in the batch, used for basic SIM-lock unlock on device insertion. Convenient for a test batch where you want to remember one PIN for all of them; for production, leave unchecked so each SIM gets a unique, randomly-generated PIN — a shared PIN means compromising one SIM's PIN effectively compromises the SIM-lock protection of the entire batch.",
  custom_puk: "Sets one shared PUK1 (8 digits) across the batch, used to reset a locked-out PIN1 after too many failed attempts. Same tradeoff as custom_pin above: fine for a test batch, a real security downgrade at production scale since one leaked PUK unlocks PIN-reset for every SIM in the batch.",
  show_iccid_breakdown: "Displays a structural breakdown of the generated ICCID showing its component fields: MII (Major Industry Identifier — the fixed prefix identifying it as a telecom SIM), Country Code, Issuer Identifier, Account/Individual Number, and the trailing Luhn checksum digit. Purely a display/verification aid — useful for confirming the generator produced a well-formed ICCID, has no effect on the actual generated credentials.",
  suci_enable: "Enables SUCI (Subscription Concealed Identifier) provisioning on the generated SIMs — the 5G privacy mechanism that encrypts the subscriber's real IMSI/SUPI before it's ever sent over the air during initial registration, so it can't be passively intercepted (unlike plain 4G, where the IMSI is sent in the clear on the very first attach in certain fallback scenarios). This project's IMS/VoLTE feature depends on subscribers being properly SUCI-provisioned and matched to a UDM home-network key, so enable this for any SIM that needs to register on the 5G side or use IMS.",
  suci_profile: "Which SUCI encryption scheme to provision onto the generated SIMs. Profile A (X25519/Curve25519) is the far more common and widely-supported choice across UDM implementations and SIM tooling — use it unless you have a specific reason to need Profile B (secp256r1/prime256v1), which exists mainly for deployments standardized on NIST P-256 elsewhere. Must match the scheme of the Home Network key pair this SIM will be validated against on the UDM side, or SUCI decryption fails outright.",
  suci_pki: "Home Network Public Key Identifier (1-255) baked into the generated SIM, telling the network which of its provisioned private keys to use when decrypting this SIM's SUCI back into a real IMSI. Must exactly match the hnet_id of an actual key pair configured in Open5GS's UDM (see the SUCI Key Management page) — a SIM provisioned with a PKI ID the UDM doesn't recognize will never successfully complete SUCI decryption, even though its IMSI/Ki/OPc are otherwise perfectly valid.",
  suci_routing: "Routing Indicator — a 4-hex-character value (0000-FFFF) that helps route a SUCI to the correct UDM instance in deployments running multiple UDMs (e.g. different UDMs per region or per MVNO). Default: 0000, which is correct and sufficient for essentially every single-UDM deployment, including this one — only change this if you're deliberately operating more than one UDM and need SUCI traffic explicitly partitioned between them.",
  suci_public_key: "The Home Network Public Key, in hex, provisioned onto the generated SIMs alongside the PKI ID and Profile scheme above. This is the public half of the asymmetric key pair whose private half lives on the UDM (see suci_pki and the SUCI Key Management page) — every SIM provisioned with this public key can only be decrypted by a UDM holding the matching private key, so this value must be copied exactly from that UDM key pair, not invented independently.",
};
