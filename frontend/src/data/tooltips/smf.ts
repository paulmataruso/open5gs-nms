// SMF (Session Management Function) Tooltips
export const SMF_TOOLTIPS = {
  sbi_address: "Bind address for the SMF's Service-Based Interface — the HTTP/2 API used for N7 (SMF↔PCF policy exchange), N11 (AMF↔SMF PDU session establishment/modification), and NRF registration/discovery. This is the SMF's core-internal signaling address, distinct from the PFCP address below (which faces the UPF, not other NFs). Use a dedicated loopback/dummy IP so it doesn't collide with any other NF's SBI on this host.",
  sbi_port: "Port for the SMF's SBI. Default: 7777, shared default with every other 5G NF — safe as long as each NF has its own bind address. Every N7/N11 call to/from this SMF instance goes over this port.",
  scp_uri: "URI of a Service Communication Proxy for indirect NF discovery/routing. Leave empty to use direct NRF-based discovery (the default and simplest choice) — only populate this if an SCP is actually deployed and you want SMF's SBI traffic proxied through it. Format: http://ip:port.",
  pfcp_server: "Bind address for the SMF's PFCP (N4) interface — the control-plane channel to the UPF over which the SMF installs, modifies, and deletes packet-forwarding/QoS rules for each active PDU session. This is a completely separate protocol and address from the SBI fields above; a working SBI doesn't imply a working PFCP link, and vice versa. Often loopback (e.g. 127.0.0.4) when SMF and UPF are colocated on the same host.",
  pfcp_port: "PFCP protocol port. Default: 8805 — must match the port the UPF's own PFCP server is listening on (see UPF's pfcp_address/pfcp_port), or the N4 association will simply never form and PDU sessions will fail at the UPF-selection step even though NGAP/registration succeeds fine.",
  upf_address: "Address of the UPF this SMF sends PFCP session-establishment/modification requests to. Must exactly match the UPF's own configured pfcp_address — a mismatch here is one of the most common causes of 'registration succeeds but data sessions never come up,' since the SMF will keep trying to reach a UPF that isn't actually listening at that address.",
  gtpc_address: "GTP-C (control-plane) address used for 4G/5G interworking — specifically the S11 interface toward an SGW-C when this SMF is also acting in a combined SMF+PGW-C role for 4G subscribers. Irrelevant to a pure 5G-only deployment with no 4G interworking configured; only set this up if you're actually running the shared 4G/5G core NF pattern documented for this project.",
  gtpu_address: "GTP-U (user-plane) address used for the S5 interface in 4G/5G interworking scenarios, forwarding user data between the 4G and 5G sides of a combined core. Like gtpc_address, only meaningful if 4G interworking is actually configured — leave at defaults otherwise.",
  session_subnet: "IPv4 address pool (CIDR notation, e.g. 10.45.0.0/16) that the UPF assigns addresses from for UE PDU sessions on this DNN/slice. Size this for your expected peak concurrent sessions with real headroom — running out of addresses in this pool means new PDU session requests get rejected even though every other part of the network is healthy. Must not overlap with any other DNN's pool or with addressing used elsewhere on the host (LAN, other tunnels).",
  session_gateway: "Gateway IP address for the session pool above — the address UEs see as their default route for this PDU session, and typically also where the UPF terminates the TUN/session interface on this host. Conventionally the first usable address in session_subnet (e.g. 10.45.0.1 for a 10.45.0.0/16 pool).",
  dns_primary: "Primary DNS server address handed to UEs during PDU session establishment (via PCO/DNS IE), used by every app on the device for hostname resolution once the session is up. If this DNS server is unreachable from the UE's actual data path, name resolution silently fails for every app on that device even though the PDU session itself looks perfectly healthy.",
  dns_secondary: "Secondary DNS server, used by UEs as a fallback if the primary is unreachable or slow to respond. Recommended to point at a genuinely independent resolver from the primary (e.g. 8.8.8.8 alongside 1.1.1.1) so a single upstream outage doesn't take down both.",
  mtu: "Maximum Transmission Unit advertised to UEs for this PDU session, in bytes. Default: 1400 — deliberately smaller than a typical Ethernet/internet MTU of 1500 to leave headroom for GTP-U/IP/UDP encapsulation overhead added between the UE and the actual internet-facing interface. Setting this too high can cause silent fragmentation or blackholed large packets on paths that don't fragment cleanly (a classic symptom: small requests work, large downloads/file transfers stall).",
  freediameter: "Path to the FreeDiameter configuration file used for the Gx interface toward a PCRF, when this SMF is also handling 4G/PCRF-based policy control instead of (or alongside) native 5G PCF/N7. Only relevant if 4G policy interworking is actually configured for this deployment.",
  ctf_enabled: "Charging Trigger Function control — governs whether the SMF emits charging trigger events for this session. auto (default and recommended): the SMF only generates charging events when charging is actually configured/required elsewhere in the pipeline, avoiding pointless overhead when nothing consumes them. yes: always generate charging events regardless of whether anything is configured to receive them — useful only when debugging the charging path itself. no: never generate them even if charging looks configured, effectively disabling charging trigger output entirely — use this to intentionally turn charging off without touching the rest of the charging configuration.",
  s_nssai_sst: "Slice Service Type (1-255) this SMF instance serves. Standard 3GPP values: 1 = eMBB (enhanced Mobile Broadband — the default for ordinary data/internet service), 2 = URLLC (Ultra-Reliable Low-Latency Communication), 3 = MIoT (massive IoT). Must match the SST a subscriber's own slice selection (URSP/subscription data) requests, or their PDU session establishment for this slice is rejected before it ever reaches UPF selection.",
  s_nssai_sd: "Slice Differentiator (optional 24-bit hex, e.g. 010203) — distinguishes multiple independent slices that share the same SST, for use cases like per-tenant isolation within one eMBB category. Leave unset unless you're deliberately running more than one slice of the same SST.",
  s_nssai_dnn: "Data Network Name(s) this slice serves — the APN-equivalent string a UE requests in its PDU Session Establishment Request to reach a specific external network or service. Common values: 'internet' (general internet breakout, the default for most subscribers), 'ims' (IMS/VoLTE signaling and media — Open5GS's ims-controller.ts provisions this as its own dedicated session pool), 'iot' (a narrower, often rate-limited network for IoT device traffic). A subscriber's own subscription data must list a matching DNN or their request for it is rejected.",
  log_path: "Filesystem path where the SMF writes its log file — PDU session establishment/modification/release, PFCP transactions with the UPF, and QoS rule changes all appear here. Parent directory must exist and be writable by open5gs-smfd, or the service fails to start.",
  log_level: "SMF log verbosity. debug is the right setting when chasing a specific session-establishment failure (shows the full PFCP exchange and SBI calls involved). info is the normal steady-state level. warn/error strip that detail down to genuine problems once the deployment is known-good.",
};
