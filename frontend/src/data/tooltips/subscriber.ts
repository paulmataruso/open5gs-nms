// Subscriber Management Tooltips
export const SUBSCRIBER_TOOLTIPS = {
  imsi: "International Mobile Subscriber Identity — 15 digits total, structured as MCC (3) + MNC (2-3) + MSIN (the remainder). Uniquely identifies this subscriber worldwide and is the primary key Open5GS uses to look up a subscriber's entire profile during attach. Must exactly match the IMSI programmed onto the physical SIM/eSIM — even a single-digit typo here means the SIM will never successfully authenticate, since the network won't find a matching subscriber record at all (not a wrong-credentials failure, a no-record-found one).",
  ki: "Authentication Key (Ki) — 32 hex characters (128 bits), the long-term secret shared between the SIM's secure element and this subscriber's HSS/UDM record. Every authentication challenge (Milenage/5G-AKA) is computed from this key on both sides independently and compared — this is the actual cryptographic root of trust for 'is this really the SIM it claims to be.' Treat this value as highly sensitive: anyone with both the Ki and the IMSI can clone the subscriber's identity. Never share, log, or export it outside secure provisioning flows.",
  opc: "Operator Variant Key (OPc) — 32 hex characters (128 bits), derived from the network operator's own root Operator Key (OP) combined with this subscriber's Ki via a one-way derivation. Used as an input to the Milenage/5G-AKA authentication algorithm alongside Ki. Must be generated consistently with whatever OP value your SIM provisioning process actually used — an OPc computed from the wrong OP will cause authentication to fail even if the Ki itself is correct, since the two values are supposed to travel together.",
  amf: "Authentication Management Field — a 16-bit hex value (default: 8000 for 5G) that's an input to the authentication vector generation, primarily used to control sequence-number (SQN) management and re-synchronization behavior between the SIM and the network. Changing this from the default is rarely necessary — it mainly matters for specific SIM vendor interoperability edge cases around SQN handling.",
  msisdn: "The subscriber's phone number in E.164 international format (e.g. +12345678900). Optional for pure data-only subscribers, but required for anything voice/SMS-related — SMS routing, MMS sender identification (this project's MM1 proxy resolves sender MSISDN from this field), and any billing/CDR system that keys off phone number rather than IMSI.",
  subscriber_status: "Service status flag controlling whether this subscriber is allowed to attach at all: 0 = Service Granted (normal, can attach and use service), 1 = Operator Determined Barring (ODB — subscriber is administratively suspended; every attach attempt is rejected at the HSS/UDM lookup step, before authentication even runs). Use this to temporarily disable a subscriber (e.g. non-payment, lost device) without deleting their record or losing their provisioned keys.",
  network_access_mode: "Controls which access types this subscriber's profile permits: 0 = Packet + Circuit (both data and legacy circuit-switched voice/SMS access are allowed — needed for CSFB fallback scenarios), 2 = Packet only (data-only; the subscriber cannot use circuit-switched fallback at all). This directly affects whether VoLTE/IMS-related fallback paths are even available to this specific subscriber, independent of whether the network itself supports them.",
  rau_tau_timer: "Periodic Routing/Tracking Area Update timer, in minutes (default: 12). Controls how often an idle (not actively communicating) UE checks back in with the network just to confirm it's still reachable, without doing a full re-attach. Lower values mean the network's picture of 'is this device still here' stays fresher — useful for presence-sensitive use cases — at the cost of more periodic signaling from the device even when it's doing nothing. Higher values reduce signaling load but mean a device that silently lost coverage takes longer for the network to notice.",
  access_restriction: "Bitfield controlling which Radio Access Technologies (2G/3G/4G/5G) this subscriber is permitted to use. Default: 32 (no restrictions — every RAT this network offers is available to the subscriber). Rarely needs changing outside specific use cases like forcing a device to a particular generation for testing, or restricting IoT devices that shouldn't ever be allowed onto more expensive/higher-capability RATs.",
  sst: "Slice Service Type (1-255) this subscriber's default or additional slice belongs to. Standard 3GPP values: 1 = eMBB (enhanced Mobile Broadband — ordinary high-throughput data, correct for essentially every regular phone subscriber), 2 = URLLC (Ultra-Reliable Low-Latency Communication — industrial/time-critical use cases), 3 = MIoT (massive IoT — high device density, low per-device throughput, often rate-limited). Must match an SST the AMF/NSSF actually advertise as supported, or the subscriber's PDU session request for that slice is rejected.",
  sd: "Slice Differentiator (optional, 24-bit hex, e.g. 0x123456) — distinguishes this subscriber's slice from another slice that happens to share the same SST, for use cases like per-tenant isolation. Leave unset unless the deployment is deliberately running multiple slices of the same SST that this subscriber needs to be routed to a specific one of.",
  default_indicator: "Marks this as the subscriber's default network slice — the one used automatically when a UE doesn't explicitly request a specific S-NSSAI. Exactly one slice entry per subscriber must be marked default; leaving none marked (or marking more than one, depending on how the UI enforces this) can cause ambiguous or failed slice selection during PDU session establishment.",
  session_name: "The Session/APN/DNN name for this profile entry (e.g. 'internet', 'ims') — the string a UE requests in its PDU Session Establishment / PDN Connectivity Request to reach a specific data network or service. Must match a DNN the core NFs are actually configured to serve (SMF's s_nssai_dnn, or the APN list on the 4G side) — requesting an unrecognized DNN gets the session rejected outright.",
  session_type: "IP version requested for this session: 1 = IPv4 only, 2 = IPv6 only, 3 = IPv4v6 (dual stack, both addresses assigned simultaneously). Actual availability depends on whether the serving UPF/PGW-U has an address pool configured for the requested type on this DNN — requesting IPv6 or dual-stack against a DNN whose pool is IPv4-only will fail or silently downgrade depending on UE/network negotiation behavior.",
  qos_index: "5QI (5G QoS Identifier) or QCI (4G QoS Class Identifier) value (1-255) defining how the network prioritizes and schedules this session's packets. Common standardized values: 1 = conversational voice (VoLTE, the tightest latency/jitter requirements), 5 = IMS signaling (SIP control traffic, not the voice media itself), 9 = default/best-effort internet traffic (what most ordinary data sessions use), 79 = low-priority/background traffic. Picking the wrong QoS index for a service either wastes premium scheduling resources on traffic that doesn't need it, or starves latency-sensitive traffic (like voice) of the treatment it actually requires.",
  arp_priority: "Allocation and Retention Priority level (1-15). Lower numbers mean higher priority — this session gets preferential treatment when the network needs to decide which sessions to admit or preserve during congestion or resource contention. A voice bearer is typically given a much lower (higher-priority) ARP value than a background data session so it survives contention that a lower-priority session wouldn't.",
  arp_preemption_capability: "Whether this session is allowed to preempt (forcibly evict) other, lower-priority sessions when resources are scarce: 1 = Enabled (this session can kick out lower-ARP-priority sessions to get the resources it needs), 2 = Disabled (this session will never bump another session out, even if it's higher priority). Typically enabled only for genuinely critical traffic like emergency calls or dedicated voice bearers.",
  arp_preemption_vulnerability: "Whether this session itself can be preempted (forcibly evicted) by a higher-priority session needing its resources: 1 = Enabled (this session can be kicked out), 2 = Disabled (protected — this session will not be evicted regardless of what else needs resources). Mark critical sessions (e.g. an active emergency call) as non-vulnerable so ordinary traffic can never bump them.",
  ambr_uplink_value: "Aggregate Maximum Bit Rate for uplink (device-to-network) traffic — the hard ceiling on this subscriber/session's total upload throughput across all their non-GBR bearers combined (GBR bearers like dedicated voice have their own separate guaranteed rate, unaffected by this cap). Paired with ambr_uplink_unit below to form the actual rate.",
  ambr_uplink_unit: "Unit multiplier applied to the uplink AMBR numeric value: 0 = bps, 1 = Kbps, 2 = Mbps, 3 = Gbps, 4 = Tbps. Getting this wrong by one step (e.g. entering Mbps but selecting Kbps) either throttles the subscriber to a fraction of the intended speed or grants them a wildly higher cap than intended — worth double-checking against the numeric value whenever this is edited.",
  ambr_downlink_value: "Aggregate Maximum Bit Rate for downlink (network-to-device) traffic — the ceiling on this subscriber/session's total download throughput across all non-GBR bearers. This is usually the number that actually matters to a subscriber's perceived speed, since most consumer usage (streaming, browsing, downloads) is downlink-heavy.",
  ambr_downlink_unit: "Unit multiplier for the downlink AMBR value: 0 = bps, 1 = Kbps, 2 = Mbps, 3 = Gbps, 4 = Tbps. Same gotcha as the uplink unit — verify the unit matches the intended magnitude of the numeric value.",
  imeisv: "IMEI Software Version — the device's hardware identity plus software-version suffix, automatically populated by the network the first time this subscriber's SIM is used in a device and attaches. Read-only/informational in practice: useful for identifying what physical device a subscriber is actually using (device model, and sometimes catching a SIM inserted into an unexpected/unauthorized device), but not something you'd normally hand-edit.",
};
