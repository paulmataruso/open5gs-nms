// SUCI Key Management Tooltips
export const SUCI_TOOLTIPS = {
  pki_id: "Public Key Identifier (1-255) — the value the UDM uses to look up which private key to try when decrypting an incoming SUCI. Must be unique among key pairs of the same Profile type (a Profile A key and a Profile B key can share the same ID since they're distinguished by scheme too, but two Profile A keys cannot). This exact value must be provisioned onto every SIM/eSIM meant to be validated against this specific key pair — see the SIM Generator's suci_pki tooltip for the matching field on the provisioning side.",
  profile: "Which SUCI Protection Scheme this key pair implements. Profile A uses the X25519 elliptic curve (Curve25519) and is by far the more common, widely-supported choice — default to this unless you have a specific interoperability reason to need Profile B (secp256r1/prime256v1, NIST P-256). The two schemes are cryptographically incompatible with each other, so every SIM provisioned against this key must use the exact same profile or SUCI decryption will simply fail.",
  key_file: "Filesystem path to this key pair's private key file, stored under /etc/open5gs/hnet/. Naming format: curve25519-{id}.key for Profile A, secp256r1-{id}.key for Profile B, where {id} is this key's pki_id. This file is genuinely sensitive — it's the only thing standing between an intercepted SUCI and a subscriber's real IMSI, so treat read access to it (and to backups containing it) as security-critical.",
  public_key: "The public half of this key pair, in hexadecimal — the value you provision onto eSIMs during personalization (paired with pki_id and profile) so they can encrypt their SUCI in a way only this UDM's matching private key can decrypt. Safe to share/export on its own (unlike the private key file), since possessing the public key alone doesn't let anyone decrypt traffic — only encrypt to this UDM.",
  regenerate_warning: "Regenerating this key pair immediately and irreversibly replaces both the public and private key with a brand-new pair. Every eSIM that was provisioned with the OLD public key becomes unable to complete SUCI decryption from that moment on — their next attach attempt will fail — until each one is individually reprovisioned with the new public key. There is no way to un-regenerate; if you're not certain every affected SIM can be reprovisioned promptly, consider creating a new key pair with a different PKI ID instead of regenerating this one in place.",
  delete_file: "When checked, also permanently deletes the private key file from disk (/etc/open5gs/hnet/) as part of removing this key pair — an unrecoverable action if you don't have a separate backup. When unchecked, the key pair is only removed from udm.yaml's configuration (so the UDM stops offering/using it) but the underlying private key file is left on disk untouched, which is the safer choice if you might want to reference or restore this key later.",
};
