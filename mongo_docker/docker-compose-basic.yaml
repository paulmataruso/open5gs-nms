version: "3.8"

services:
  mongodb:
    image: mongo:4.4
    container_name: mongodb
    network_mode: host
    command: ["--bind_ip", "0.0.0.0"]
    ports:
      - "0.0.0.0:27017:27017"
    volumes:
      - ./mongodb_data:/data/db
